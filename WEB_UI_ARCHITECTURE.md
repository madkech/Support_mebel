# Web UI Architecture — Kitchen Calculator MVP

> Зафиксировано: 2026-05-13
> Статус: ✅ Утверждён как MVP-план
> Стек: Vite + Vanilla TypeScript + DOM API (без фреймворков)

---

## 🖥 Архитектура веб-интерфейса

### Структура папок

```
web-ui/
├── index.html                 # Точка входа
├── package.json               # vite, typescript
├── tsconfig.json              # TS-конфиг для браузера
├── vite.config.ts             # Алиасы к calculator core
│
├── public/
│   └── favicon.svg
│
└── src/
    ├── main.ts                # Bootstrap приложения
    ├── app.ts                 # Контроллер: форма → расчёт → вывод
    ├── bridge.ts              # Мост к ядру src/calculators/kitchenCalculator.ts
    │
    ├── components/
    │   ├── input-form.ts      # Форма ввода всех параметров
    │   ├── results-panel.ts   # Панель результатов
    │   ├── module-table.ts    # Таблица модулей с деталями
    │   ├── rules-log.ts       # Применённые правила + предупреждения
    │   └── layout-scheme.ts   # Визуальная схема планировки (боксы)
    │
    ├── styles/
    │   ├── base.css           # Сброс, переменные, типографика
    │   ├── form.css           # Стили формы
    │   ├── results.css        # Стили результатов
    │   └── scheme.css         # Стили визуальной схемы
    │
    └── utils/
        ├── labels.ts          # Русские метки (переиспользуем из cli.ts)
        └── format.ts          # Форматирование мм, материалов
```

---

## 🧩 Дерево компонентов

```
┌─────────────────────────────────────────────────────────┐
│  App                                                    │
│  ┌──────────────────────┬──────────────────────────────┐│
│  │  InputForm            │  ResultsPanel                ││
│  │  ┌──────────────────┐ │  ┌─────────────────────────┐ ││
│  │  │ Section: Стена   │ │  │ SummaryBar (сводка)     │ ││
│  │  │  wallLengthMm    │ │  │  3000×2700, 5 модулей   │ ││
│  │  │  ceilingHeightMm │ │  └─────────────────────────┘ ││
│  │  └──────────────────┘ │  ┌─────────────────────────┐ ││
│  │  ┌──────────────────┐ │  │ LayoutScheme (схема)    │ ││
│  │  │ Section: Опции   │ │  │ ┌──┬──┬──┬──┬──┐ верх  │ ││
│  │  │  ☑ Gola-профиль  │ │  │ │суш│ст│вт│свч│ст│      │ ││
│  │  │  ☑ Посудомойка   │ │  │ ├──┼──┼──┼──┼──┤ низ   │ ││
│  │  │  ☑ СВЧ           │ │  │ │мо│пм│вр│ящ│ст│       │ ││
│  │  │  ☐ Вытяжка (газ) │ │  │ └──┴──┴──┴──┴──┘      │ ││
│  │  └──────────────────┘ │  └─────────────────────────┘ ││
│  │  ┌──────────────────┐ │  ┌─────────────────────────┐ ││
│  │  │ Section: Размеры  │ │  │ ModuleTable (детали)   │ ││
│  │  │  Фартук: [580]   │ │  │ Мод.1 мойка 600×782×500│ ││
│  │  │  Глубина: [510]  │ │  │  ├ боковина 2шт 766×500│ ││
│  │  │  Ширина мод: [600]│ │  │  ├ дно 1шт 600×500    │ ││
│  │  └──────────────────┘ │  │  └ планка 1шт 568×100  │ ││
│  │  ┌──────────────────┐ │  │ ...                     │ ││
│  │  │ [Рассчитать ▸]   │ │  └─────────────────────────┘ ││
│  │  └──────────────────┘ │  ┌─────────────────────────┐ ││
│  └──────────────────────┴─│  │ RulesLog                │ ││
│                           │  │ ✓ фартук: 580 мм        │ ││
│                           │  │ ✓ база: 920 мм (Gola)   │ ││
│                           │  │ ⚠ остаток распред.      │ ││
│                           │  └─────────────────────────┘ ││
│                           └──────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Поток данных

```
  InputForm (состояние)
       │ KitchenProjectInput
       ▼
  bridge.ts → calculateKitchenProject()
       │ KitchenProject
       ▼
  App State (единый объект)
       │
  ┌────┼────┬──────────┐
  ▼    ▼    ▼          ▼
Summary ModuleTable RulesLog
Bar   LayoutScheme
```

---

## 🎨 Ключевые UI-решения

| Элемент | Реализация |
|---------|------------|
| **Форма** | Секции с группами полей, селекты для типов, чекбоксы для опций |
| **Схема** | CSS Grid: верхний ряд (навесные) + нижний (нижние), цветом по типу |
| **Таблица деталей** | Аккордеон: модуль раскрывается → список деталей |
| **Правила** | Зелёные ✓ (применено), жёлтые ⚠ (предупреждения) |
| **Экспорт** | Кнопка «Скачать CSV» / «Печать» |
| **Адаптив** | На десктопе 2 колонки, на мобильном — форма сверху |

---

## 🔌 Мост к ядру (`bridge.ts`)

```typescript
import { calculateKitchenProject } from '../../src/calculators/kitchenCalculator.ts';
import type { KitchenProjectInput, KitchenProject } from '../../src/types/kitchen.ts';

export function calculate(input: KitchenProjectInput): KitchenProject {
  return calculateKitchenProject(input);
}
```

---

## 📦 Ключевые зависимости (`package.json`)

| Пакет | Версия | Назначение |
|-------|--------|------------|
| vite | ^6.0.0 | Сборщик + dev-сервер |
| typescript | ^5.7.0 | Проверка типов |

---

## 🗺 План реализации

| # | Компонент | Файл | Сложность |
|---|-----------|------|-----------|
| 1 | Bootstrap Vite-проекта | `web-ui/` | ⭐ |
| 2 | Мост к ядру | `src/bridge.ts` | ⭐ |
| 3 | Форма ввода | `src/components/input-form.ts` | ⭐⭐⭐ |
| 4 | Панель результатов | `src/components/results-panel.ts` | ⭐⭐ |
| 5 | Таблица модулей | `src/components/module-table.ts` | ⭐⭐⭐ |
| 6 | Визуальная схема | `src/components/layout-scheme.ts` | ⭐⭐ |
| 7 | Правила и предупреждения | `src/components/rules-log.ts` | ⭐ |
| 8 | Стилизация | `src/styles/*.css` | ⭐⭐ |
| 9 | Утилиты (метки, формат) | `src/utils/*.ts` | ⭐ |

---

## 💡 Принципы

1. **Ноль фреймворков** — чистый TypeScript + DOM API
2. **Ядро неприкасаемо** — `src/calculators/` импортируется как есть, без изменений
3. **Русский язык** — весь UI на русском (метки из `cli.ts`)
4. **Один проход** — одна кнопка «Рассчитать», сразу всё
5. **Печать** — @media print для спецификации
